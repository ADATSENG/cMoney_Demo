const aliases = (prefix = `src`) => ({
    '@components': `${prefix}/components`,
    '@pages': `${prefix}/pages`,
    '@styles': `${prefix}/styles`,
    '@utils': `${prefix}/utils`,
  });
  
  module.exports = aliases;